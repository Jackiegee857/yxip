name: Update IP List

on:
  schedule:
    - cron: '0 1,13 * * *'  # 每天UTC 1:00和13:00
  workflow_dispatch:
  push:

jobs:
  update-ip-list:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0

    - name: Set Git identity
      run: |
        git config --global user.email "tianshideyou@proton.me"
        git config --global user.name "tianshipapa"
        git config --global pull.rebase false
        git config --global --add safe.directory $GITHUB_WORKSPACE

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        pip install requests beautifulsoup4

    - name: Run script
      run: python ${{ github.workspace }}/collect_ips.py

    - name: Commit and push changes
      run: |
        if [ -n "$(git status --porcelain -- ip.txt)" ]; then
          echo "📄 检测到 IP 文件更新，准备提交..."
          git add ip.txt
          git commit -m "Automatic update: $(date -u +'%Y-%m-%dT%H:%M:%SZ')" || echo "⚠️ 无需提交"
          
          # 拉取最新，避免冲突
          git fetch origin main
          git merge --strategy-option theirs origin/main || true
          
          # 推送
          git push origin HEAD:main
          echo "✅ 更新推送成功"
        else
          echo "🔄 未检测到IP变化"
        fi
